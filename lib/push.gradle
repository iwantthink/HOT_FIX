apply plugin: 'maven'

group 'com.ryan.log'

uploadArchives {
    repositories {
        mavenDeployer {
            repository(url: uri('../repo'))
            pom.version = '1.1.3'
            pom.artifactId = "Log"
        }
    }
}