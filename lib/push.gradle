apply plugin: 'maven'

group 'com.ryan.log'

uploadArchives {
    repositories {
        mavenDeployer {
            repository(url: uri('../repo'))
            pom.version = '1.0.4'
            pom.artifactId = "Log"
        }
    }
}